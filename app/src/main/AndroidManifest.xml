<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.nelsondaniel.music_app_structure">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ArtistsActivity" android:label="Artists" />
        <activity android:name=".SongsActivity" android:label="Songs"/>
        <activity android:name=".NowPlayingActivity" android:label="Now Playing"/>
        <activity android:name=".PlaylistsActivity" android:label="Playlists"/>
        <activity android:name=".DiscoverActivity" android:label="Discover"/>
        <activity android:name=".UpgradeActivity" android:label="Upgrade"/>
    </application>

</manifest>